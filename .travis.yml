sudo: required
language: python

services:
  - docker

before_install:
  - git remote set-branches --add origin master
  - git fetch

jobs:
  include:
    # Building
    - stage: build
      script: ./scripts/is_change_in_subproject.py backend && cd backend && docker-compose build

